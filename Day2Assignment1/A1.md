** structs, mappings and arrays are stored in storage, arrays can be stored in memory as well
** mapping is stored in storage by default and can only be in storage, so no need to add the storage name.